# Place this file at the repository root as .env.local (same folder as vite.config.ts).
# Vite loads env files from the repository root, not client/.
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-public-publishable-or-anon-key
